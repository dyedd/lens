interface:
  display_name: "Lens Testing"
  short_description: "Keep behavior tests in CI while local checks stay fast"
  default_prompt: "Apply the Lens testing policy: add only behavior coverage, run focused local checks, and leave the full parallel suite to CI."
