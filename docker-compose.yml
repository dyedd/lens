services:
  app:
    image: ghcr.io/dyedd/lens:latest
    container_name: lens
    restart: unless-stopped
    env_file:
      - path: .env
        required: true
    ports:
      - "${LENS_HOST_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
